Read progress.md, pick the next file for convertion and convert it to Rust. During conversion try to follow your recommende directory structure in rewrite-plan.md After
  convertion is done, verify that build compiles. If conversion was successfull, create a commit, but don't push it.




Read progress.md, pick the next file for convertion and convert it to Rust. During conversion try to follow your recommended directory structure in rewrite-plan.md.
After convertion is done, verify build with 'cargo build' and 'cargo test'. If conversion was successfull, create a commit, but don't push it.
Then continue to next files until the Phase 1 from progress.md is not finished.
